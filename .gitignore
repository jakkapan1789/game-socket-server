# Ignore node_modules directory
node_modules/

# Optional: Ignore package-lock.json (if you don't want it in the repo)
# Uncomment the following line if needed:
# package-lock.json

# Optional: Ignore environment files (common practice)
.env

# Optional: Ignore build directories (e.g., for React or Next.js projects)
dist/
build/
